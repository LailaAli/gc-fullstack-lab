const INITIAL_STATE = {
    loading: true,
    buyItems: [ ]
};

export default function (state = INITIAL_STATE, action) {
    switch (action.type) {

        default:
            return state;
    }
}
